### Codex Audit Table

Codex does a second independent pass over the same files in the same order.
Add rows here as Codex reads each file. Same column structure as the Claude table.

**Codex instructions**: Read `context.md` for the session protocol (batch sizes, update cadence,
synthesis requirements). Follow the same "NEXT" file list from the Claude pass — same order,
same groupings. Add your findings here, not to Claude's table. If you confirm a Claude finding,
note "Confirmed: [issue]". If you find something Claude missed, note it fresh.

| File | Lines | Purpose | Tests | Issues | Action Needed |
|------|-------|---------|-------|--------|---------------|
| `engine/types.rs` | 53 | Canonical enums for role/zone/compression/pinning shared across engine state and serde payloads. | None (correct for pure type definitions) | None; modeling is cohesive and serde annotations are explicit. | None |
| `engine/block.rs` | 74 | Core `Block` data model plus compression/version and provider metadata payload structures. | None (correct; data-only) | `BlockMetadata` currently has `tool_name` but no `tool_use_id`; this is the missing key needed for unambiguous ToolUse→ToolResult linkage downstream. Confirmed risk against Claude pass: this file’s schema is where the dependency bug fix must land. | Add `tool_use_id: Option<String>` to `BlockMetadata` during execution phase and thread it through parser + dependency graph construction tests. |
| `engine/zone.rs` | 391 | Token-budget zone assignment (`assign_zones`) plus context ordering helpers. | **248 lines inline `#[cfg(test)]`** | `assign_zones()` line 83 sorts/assigns at block granularity, not turn granularity; when recency budget boundary lands inside a turn, one exchange can be split between Recency and Middle. This is deterministic (Rust `sort_by` is stable), so Claude’s “unstable sort nondeterminism” claim is not confirmed; the real issue is policy semantics: recency logic should likely preserve whole-turn coherence. | Extract tests to `engine/tests/zone_tests.rs`. Decide policy: if turn atomicity is required, accumulate by `turn_index` groups then assign entire turns together. |
| `engine/session.rs` | 401 | Concurrent `SessionStore` with active-session tracking and anti-steal guard for small auxiliary sessions. | **142 lines inline `#[cfg(test)]`** | `SessionStore::create()` lines 151-153 allows active-session flip on `same_model` (provider+model), even when the current active session is large. In multi-conversation same-model use, a new session can steal active status despite guard intent (“tiny auxiliary sessions shouldn’t steal active”). Confirmed Claude note: `source` is free-form and undocumented. | Extract tests to `engine/tests/session_tests.rs`. Tighten activation predicate (prefer thread/session affinity over provider+model equality) and document allowed `source` values. |
| `engine/store.rs` | 399 | `DashMap`-backed block CRUD/query/aggregate store for hot-path context state. | **220 lines inline `#[cfg(test)]`** | `replace_all()` line 162 is documented as “atomically (clear + insert)” but is not atomic under concurrent readers: readers can observe empty/partial map between `clear()` and reinserts. This can transiently break classification/export callers in concurrent flows. Confirmed Claude notes: `tokens_by_zone()` uses `HashMap<String, u32>` while `tokens_by_role()` is strongly typed; `all_ordered()` assumes ISO-8601 lexicographic timestamp ordering. | Extract tests to `engine/tests/store_tests.rs`. Either (a) fix docs to “best-effort replace,” or (b) implement true snapshot swap (e.g., `ArcSwap`/RwLock-owned map replacement) if atomic visibility is required. Consider consistent aggregate key typing. |
| `engine/budget.rs` | 175 | Budget utilization classification and default context-window limits by model family. | **58 lines inline `#[cfg(test)]`** | Confirmed Claude concern: `default_token_limit()` lines 89-112 uses broad prefix matching (`starts_with("codex")`, `starts_with("gpt-5")`, etc.), so any future model variant silently inherits current limits. This can over/under-budget new models until manually updated. | Extract tests to `engine/tests/budget_tests.rs`. Centralize limits in a versioned model-capability map and add an explicit “unknown model” warning path. |
| `engine/staleness.rs` | 182 | Staleness scoring/ranking utility for compression candidate selection. | **108 lines inline `#[cfg(test)]`** | Confirmed Claude note: `rank_by_staleness()` line 69 resolves `partial_cmp` failure as `Equal`, masking NaN ordering anomalies instead of surfacing them. Also no invariant docs for allowable `usage_heat` range, which governs denominator safety in `calculate_staleness()`. | Extract tests to `engine/tests/staleness_tests.rs`. Add invariant docs (or clamping) for `usage_heat`, and consider a defensive finite-check before sorting. |
| `engine/tokens.rs` | 239 | Model-aware token counting via `tiktoken-rs` with lazy tokenizer initialization and heuristic fallback. | **138 lines inline `#[cfg(test)]`** | Confirmed Claude smell: `recount_block_tokens()` (line 98) is a pass-through wrapper over `count_tokens()`. Additional maintainability concern: tokenizer initialization failures silently downgrade to heuristic counts (lines 81-94), which can hide misconfiguration during production runs. | Extract tests to `engine/tests/token_tests.rs`. Remove redundant wrapper and add one-time warning/logging on tokenizer init fallback so operators can detect degraded counting accuracy. |
| `engine/dependency.rs` | 371 | Dependency graph primitives plus batch dependency inference (conversation/tool/file edges). | **129 lines inline `#[cfg(test)]`** | Confirmed Claude bug: `build_dependencies()` lines 171-174 links `ToolUse` to `ToolResult` by `tool_name` and `turn_index` only, so repeated tool invocations can mispair. Confirmed Claude doc mismatch: `find_orphans()` line 100 says “file dependencies” but evaluates every edge type. Additional precision gap: tool-result search uses first matching element in input order, not nearest/unique match by explicit ID, so ordering artifacts can alter pairing even before repeated-name collisions. | Extract tests to `engine/tests/dependency_tests.rs`. Add `tool_use_id` to metadata and match ToolChain primarily by ID; update orphan docs to cover all dependency kinds; add regression tests for repeated tool names and out-of-order input blocks. |
| `engine/versioning.rs` | 253 | Per-block version snapshot store (`VersionStore`) for undo/history operations. | **79 lines inline `#[cfg(test)]`** | Confirmed Claude smell: local `iso_now()` wrapper duplicates `crate::util::iso_now()` usage pattern seen in other files. No correctness bug found in retention/undo mechanics at this stage. | Extract tests to `engine/tests/versioning_tests.rs`. During cleanup pass, remove duplicated local time wrappers across engine modules. |
| `engine/action_log.rs` | 316 | Mutex-protected bounded mutation log (`ActionRecord`) for audit and undo discovery. | **88 lines inline `#[cfg(test)]`** | Confirmed Claude issue: `new_record()` hardcodes `session_id: None` (line 206), forcing every caller to patch session linkage manually if needed. Confirmed duplicated `iso_now()` wrapper pattern. | Extract tests to `engine/tests/action_log_tests.rs`. Add `session_id` parameter (or builder helper) to `new_record` and remove local time wrapper duplication. |
| `engine/policy.rs` | 296 | Static rule-chain policy engine for mutation guardrails and confirmation flows. | **116 lines inline `#[cfg(test)]`** | Confirmed Claude notes: `PolicyRule.check` as bare fn pointer blocks stateful rule capture; `protect_pinned()` has redundant `unwrap_or` after `block.pinned?` short-circuit. No immediate behavioral bug in rule precedence. | Extract tests to `engine/tests/policy_tests.rs`. Keep fn-pointer approach only if intentional; otherwise migrate to trait object/enum rule strategy when extensibility work starts. |
| `engine/pipeline.rs` | 384 | Three-pass classification pipeline (zone assignment, heuristics, staleness-pruning suggestions). | **172 lines inline `#[cfg(test)]`** | Confirmed Claude bug: `HeuristicResult.recency_boost` is computed but never consumed by `classify()`. Additional concern: pruning candidates currently exclude only pinned blocks (line 88), so `Role::System` blocks can be suggested for pruning if unpinned, conflicting with expected “system prompt protection” semantics unless filtered downstream. | Extract tests to `engine/tests/pipeline_tests.rs`. Remove or wire `recency_boost`; verify pruning candidate contract and explicitly exclude system-role blocks if policy expects non-prunable system context. |
| `engine/ingest.rs` | 297 | Main `ContextEngine::ingest` path: normalize, retokenize, replace session blocks, classify, rebuild deps, log, persist. | No inline tests (covered by `engine/tests.rs`) | Confirmed Claude bug: `block_semantic_fingerprint()` uses `DefaultHasher` (line 255), producing non-deterministic fingerprints across process runs. Additional robustness gap: `is_regressive_semantic_collapse()` relies on `HashSet<u64>` uniqueness, so duplicate-content blocks collapse to one fingerprint and can undercount removals in repetition-heavy sessions. | Replace fingerprint hasher with deterministic one (`FxHasher` or explicit stable hash impl). Add regression tests for duplicate semantic blocks to validate collapse guard behavior. |
| `engine/session_sync.rs` | 79 | `ContextEngine` persistence bridge for active-session sync and UI count/event emission. | None (impl helper surface; behavior covered through engine integration tests) | Confirmed Claude finding: `refresh_active_session_totals()` lines 23-36 does read/filter/write across `store` and `sessions` without a shared snapshot, so concurrent mutations can write stale totals or miss recently-added IDs. Confirmed Claude finding: `active_session_counts()` lines 60-67 falls back to global counts when no active session, which can over-report in multi-session warmup paths. Codex-unique: `persist_session()` lines 43-50 saves session header then blocks in separate calls; a failure between them can persist mismatched `sessions.total_tokens` vs block rows. | No test extraction. During A.4, document the fallback semantics and decide whether these helpers need stronger consistency guarantees (transactional save path and/or single-snapshot refresh). |
| `engine/storage.rs` | 588 | SQLite persistence backend for sessions, block snapshots, and block versions. | **189 lines inline `#[cfg(test)]`** (7 tests) | Confirmed Claude architecture gap: `load_sessions()` line 154 hardcodes `block_ids: Vec::new()` and there is no `load_blocks()`; persisted block content is write-only for runtime recovery. Confirmed Claude bug: `delete_session()` lines 335-356 performs three DELETEs outside a transaction, so crash/interruption can leave partial cleanup. Confirmed Claude concern: enum persistence uses `format!("{:?}").to_lowercase()` (lines 183/187/295), coupling DB encoding to debug names. Confirmed Claude concern: `parse_edit_source()` line 395 silently maps unknown strings to `User`, losing provenance. Confirmed Claude concern: duplicated INSERT logic in `save_block()` and `save_blocks()` (lines 177-197 and 236-256). Codex-unique bug: `load_block_ids()` line 268 orders only by `turn_index`; multiple blocks in the same turn get nondeterministic row order on reload, which can reorder same-turn message fragments. | Extract tests to `engine/tests/storage_tests.rs`. Add transaction in `delete_session()`, consolidate insert SQL to one helper, and make enum serialization explicit. Add deterministic secondary ORDER BY (`turn_index, id` or persisted sequence) for `load_block_ids()` and warn on unknown `EditSource` parse values. |
| `engine/mod.rs` | 937 | Core engine coordinator exposing query APIs, policy-checked mutations, session lifecycle, persistence sync, and planner/system internal mutation hooks. | Tests delegated to `engine/tests.rs` via `#[cfg(test)] mod tests;` | Confirmed Claude finding: `ContextEngine` subsystem fields are `pub` (lines 49-55), enabling policy bypass through direct `store`/`versions`/`dependencies` mutation. Confirmed Claude finding: `undo_block()` lines 718-734 mutates state but does not append an action-log record, making undo invisible in audit history. Confirmed Claude finding: `compress_block()` lines 406-453 only flips `compression_level` and never rewrites content/tokens; name/contract is misleading vs `apply_compression_summary_internal()`. Confirmed Claude finding: `bulk_remove()` reuses a single `confirmed` flag for both bulk and per-block checks (lines 571-605), so “confirmed bulk remove” can still be denied per item after consuming confirmation intent. Codex-unique bug: `clear_all_sessions()` lines 689-694 logs DB delete failures but still clears all in-memory state; if SQLite deletion partially fails, sessions/blocks can reappear after restart (memory/DB divergence). | Restrict field visibility (`pub(crate)` or targeted accessors), add undo action logging, and either rename/document `compress_block()` or make it apply content changes. Keep two-phase bulk policy behavior explicit in UX docs. For clear-all, fail closed (abort clear on DB delete error) or make clear best-effort semantics explicit and surface partial-failure status to callers. |
| `engine/tests.rs` | 1460 | Engine integration test suite (51 tests) covering ingest guards, session identity/flip behavior, policy mutations, internal mutation APIs, overhead accounting, and compression settings wiring. | Test module file itself (no nested inline `#[cfg(test)]`) | Confirmed Claude finding: monolithic 1460L flat test file is difficult to navigate; split by concern is warranted. Codex-unique test bug: `test_ingest_session_tokens_reflect_latest()` line 285 asserts `session2.total_tokens != tokens_after_first + session2.total_tokens`, which is tautologically true for any non-zero `tokens_after_first` and does not validate non-accumulation. Codex-unique reliability issue: session-flip guard tests at lines 1364/1408/1437 use `ContextEngine::new(None)` (real SQLite path) instead of `new_in_memory()`, introducing host-state side effects and potential cross-test contention. | Split into `engine/tests/{mod,ingest_tests,session_tests,mutation_tests}.rs` as Claude mapped. Fix tautological assertion to compare expected latest-token value directly. Convert DB-touching tests to `new_in_memory()` (or isolate via temp DB path) so test outcomes are deterministic and side-effect free. |
| `engine/compression/mod.rs` | 219 | Compression backend/model settings contract with provider-aware default routing and normalization. | **86 lines inline `#[cfg(test)]`** (5 tests) | Confirmed Claude finding: `default_model_for_backend()` line 106 includes `Auto` arm, masking misuse where callers should resolve backend first. Confirmed Claude finding: unknown providers in `default_backend_for_provider()` line 99 silently fall back to Anthropic. Codex-unique bug: `default_backend_for_provider()` lines 95-100 does not recognize `"openrouter"`, so Auto mode on an OpenRouter upstream misroutes to Anthropic backend/model. | Extract tests to `engine/tests/compression_tests.rs`. Make `Auto` in `default_model_for_backend()` unreachable or explicit error path. Add `"openrouter"` mapping and decide explicit fail behavior for unknown providers (Disabled vs Anthropic fallback). |
| `engine/compression/queue.rs` | 201 | Thread-safe in-memory queue for pending/completed compression jobs used by future worker runtime. | **52 lines inline `#[cfg(test)]`** (3 tests) | Confirmed Claude finding: completed queue is unbounded (`mark_completed`/`mark_failed` lines 107-124), allowing unbounded memory growth. Confirmed Claude finding: no queue clear/drain API, leaving pending jobs alive across session clears. Confirmed Claude finding: `CompressionTargetWire` duplicates `CompressionTarget` with near-identical representation. | Extract tests to `engine/tests/compression_tests.rs`. Add fixed-cap eviction policy for completed jobs and a queue reset API. Evaluate collapsing `CompressionTargetWire` into a serde-enabled `CompressionTarget` type. |
| `engine/compression/provider.rs` | 166 | Provider trait and fail-open helper with stub/failing implementations for compression integration scaffolding. | **57 lines inline `#[cfg(test)]`** (3 tests) | Confirmed Claude finding: `CompressionProvider::compress()` is synchronous (line 40), so real networked providers require a future breaking async migration. Confirmed Claude finding: `StubProvider` fixed 120-char truncation (line 68) ignores `CompressionSettings.max_tokens`, reducing test realism. | Extract tests to `engine/tests/compression_tests.rs`. Document async migration plan for provider trait and align stub output sizing with settings-based token budget. |
| `engine/planner/types.rs` | 454 | Planner type contracts for mutations, signals, pending plan state, manifest/cleanup payloads, and planner config thresholds. | **146 lines inline `#[cfg(test)]`** (8 tests) | Confirmed Claude finding: `ContextMutation::UpdateContent` (line 53) is omitted from serde round-trip coverage. Confirmed Claude finding: `HeuristicSignals.budget_status` optionality (line 95) is inconsistent with non-optional `PlannerInput.budget`. Codex-unique contract asymmetry: `ContextMutation` includes `UpdateContent`, but `PlanActions` (lines 160-184) has no corresponding user-facing field; this is likely intentional (system-generated mutation only) but undocumented and easy to misinterpret as an API omission. | Extract tests to `engine/planner/tests/types_tests.rs`. Add `UpdateContent` serde coverage and clarify whether `UpdateContent` is intentionally internal-only in `PlanActions` docs/comments. |
| `engine/planner/validation.rs` | 342 | Planner action validation and projection utilities (`validate_plan`, mutation merge, token/block-count estimation). | No inline tests (covered by planner integration tests) | Confirmed Claude finding: dead no-op loop in `estimate_token_delta()` lines 307-312. Confirmed Claude finding: `summary.len()/4` compress estimate at line 326 is an inaccurate character heuristic. Confirmed Claude finding: recall validation path has no explanatory comment even though it intentionally skips active-block existence checks. Confirmed Claude finding: projection math duplicated between `validate_plan()` and `project_pending_plan_from_mutations()`. | Keep tests in planner suite; during A.4 remove dead loop (or emit warning), unify projection math helper, and switch compress estimation to model-aware token counting where feasible. |
| `engine/planner/relevance.rs` | 268 | File-overlap relevance boosts and turn-to-turn task-boundary detection utilities. | **155 lines inline `#[cfg(test)]`** (13 tests) | Confirmed Claude finding: `contains()` lookups in lines 34 and 58 are O(n) per element, producing O(m×n) behavior with larger file sets. Confirmed Claude finding: no path normalization, so equivalent paths with different textual forms miss matches. Confirmed Claude finding: empty previous-file set always yields task-boundary=true when current is non-empty. | Extract tests to `engine/planner/tests/relevance_tests.rs`. Precompute file-set hash lookups, document/normalize path expectations, and decide whether initial-turn boundary behavior should be special-cased. |
| `engine/planner/manifest.rs` | 564 | Manifest text generation (`status`, `delta`, `full`, `warning`) and delta synthesis for context-awareness injection. | **215 lines inline `#[cfg(test)]`** (12 tests) | Confirmed Claude finding: `generate_status_line()` line 95 hardcodes `archived_count = 0`, leaving dead/unreachable branch lines 115-117. Confirmed Claude finding: `truncate_preview()` line 345 slices bytes by character count and can panic on UTF-8 boundaries. Confirmed Claude finding: `generate_warning()` lines 229-232 only considers Middle-zone candidates and ignores Recency opportunities. Confirmed Claude finding: debug-string formatting for zones/roles/compression levels (lines 73/318/322) is brittle. | Extract tests to `engine/planner/tests/manifest_tests.rs`. Remove dead archived branch (or pass archived count in), switch preview truncation to char-boundary-safe slicing, and broaden warning candidate scan beyond Middle-only blocks. |
| `engine/planner/file_tracker.rs` | 559 | File mutation detector mapping tool call traffic into file-level mutations, then translating affected file references into `ContextMutation` updates/archives. | **319 lines inline `#[cfg(test)]`** (17 tests) | Confirmed Claude finding: `generate_file_update_mutations()` lines 108-113 emits no mutation for `Rename`, leaving old-path blocks stale until separately re-read. Confirmed Claude finding: edit parsing at lines 170-173 uses `call.result` only; it ignores `new_content` in args for tools that return structured/no result payloads. Confirmed Claude finding: `find_blocks_for_file()` line 71 uses exact string equality, so path aliases (`src/a.rs` vs `./src/a.rs`) miss. Confirmed Claude finding: `extract_file_path()` treats `file_name` as a one-off branch (lines 201-206) instead of part of the canonical key set. Codex-unique bug: rename parse currently requires old path under generic keys (`file_path`/`path`/etc.) and does not accept common `old_path`/`source`/`from` keys, so valid rename tool payloads can be dropped at line 163 before mutation creation. | Extract tests to `engine/planner/tests/file_tracker_tests.rs`. For rename, either archive old-path blocks or add explicit rename mutation semantics and document stale-read behavior. Add argument-content fallback for `Edit`, normalize file paths before matching, fold `file_name` into main key list, and extend old-path extraction keys for rename calls. |
| `engine/planner/heuristics.rs` | 1221 | Autonomous archival heuristics and suggestion generation under budget pressure, staleness, and relevance gating. | **833 lines inline `#[cfg(test)]`** (~68%, 17 tests) | Confirmed Claude finding: `apply_budget_pressure()` derives `current_turn` from max block turn (lines 180-184) while staleness path uses `signals.current_turn` (line 315), creating divergence when active turn has no block yet. Confirmed Claude finding: `apply_staleness_archival()` unconditionally excludes tool lifecycle blocks (lines 300-301) without documenting why this differs from budget-pressure behavior. Confirmed Claude finding: `collect_archival_candidates()` can omit blocks that `apply_heuristics()` would archive, so suggestion and execution coverage diverge. Codex-unique bug: `is_archival_candidate()` doc says recency is excluded (line 330) but code only excludes Primacy (lines 355-358); under soft pressure (`middle_only = false` line 239), recency blocks can be archived despite stated protection semantics. | Extract tests to `engine/planner/tests/heuristics_tests.rs`. Use `signals.current_turn` consistently in budget pressure. Document or align tool-block treatment across paths. Reconcile suggestion-vs-execution candidate policy. Add an explicit recency exclusion check (or update docs + tests if recency archival is intentionally allowed at soft pressure). |
| `engine/planner/cleanup.rs` | 1212 | Provider-format cleanup and manifest injection: strips context tool cycles from Anthropic/OpenAI/Responses payloads, compacts empties, and renders mutation breadcrumbs. | **694 lines inline `#[cfg(test)]`** (~57%, 22+ tests) | Confirmed Claude bug: `inject_manifest_openai()` lines 499-516 only prepends when existing system `content` is string; if system content is array/object it inserts a second system message instead of updating the existing one. Confirmed Claude smell: `generate_breadcrumb()` line 63 uses Debug formatting for zone text (`{target_zone:?}`), coupling user-facing output to enum debug names. Confirmed Claude architecture smell: manifest injection helpers live in cleanup module despite manifest ownership living in `manifest.rs`. Codex-unique robustness gap: `inject_manifest_openai()` only inspects the first message for `role=system`; if a valid system message appears later (non-canonical but possible after rewrites), it still inserts a new system message at index 0, duplicating system role content. | Extract tests to `engine/planner/tests/cleanup_tests.rs`. Make OpenAI manifest injection support non-string system content and target an existing system message by role scan (not index-0 only). Replace Debug formatting in breadcrumb with stable display mapping for zones. Consider co-locating manifest generation + injection helpers under planner manifest module. |
| `engine/planner/applicator.rs` | 812 | Planner mutation applicator that maps `ContextMutation` to payload rewrite decisions (turn removals/replacements/stubs) plus engine-side state updates. | **514 lines inline `#[cfg(test)]`** (~63%, 19 tests) | Confirmed Claude finding: `content_replacements` is keyed by `turn_index` (lines 140-160), so multiple `Compress`/`UpdateContent` mutations targeting different blocks in the same turn overwrite each other (last write wins). Confirmed Claude finding: `ContextMutation::Pin` always maps to `PinPosition::Top` (line 202), leaving no path to bottom pinning. Confirmed Claude finding: `format_archive_stub()` duplicates token-formatting logic used elsewhere. Confirmed Claude finding: fallback turn grouping from `request_blocks` to `engine_blocks` (lines 239-251) is defensive and under-tested. Codex-unique bug: archival dominance is only enforced for `engine_updates` (lines 286-293), not for `content_replacements`; if a block is both archived and compressed/updated in a partial-turn scenario, conflicting payload instructions can be emitted for the same turn (replacement + stub), violating the “archive dominates” contract. | Extract tests to `engine/planner/tests/applicator_tests.rs`. Consider content replacements keyed by block identity (or structured per-turn per-content-index map) instead of plain turn index. Extend `Pin` mutation to carry `PinPosition`. Deduplicate token-format helpers. Add regression test for archive+compress/update on same block in partial-turn turns and enforce dominance for payload decisions as well as engine updates. |
| `engine/planner/mod.rs` | 729 | Planner coordinator and session-state manager: staged/pending plan lifecycle, persistent archive intent, pressure warning generation, signal construction, and manifest/cleanup assembly. | `#[cfg(test)] mod tests;` (external suite in `engine/planner/tests.rs`) | Confirmed Claude finding: R9-DIAG `warn!` remains on pending-plan hot path and should be downgraded after regression verification. Confirmed Claude finding: two-step `commit_staged_plan_for_session()` + `add_persistent_archives_for_session()` API creates atomicity/call-order risk for archive persistence. Confirmed Claude finding: `is_batch_point*` ordering contract is implicit and currently test-only/stale in runtime flow. Codex-unique bug: `check_alert_level_change_for_session()` warning text reports `ceiling_pct` as the soft/medium/hard threshold percentage, so custom ceilings produce misleading threshold text. Codex-unique risk: `session_states` map is unbounded and only broadly cleared, allowing stale per-session planner state growth in long-lived multi-session usage. | Add threshold-message tests under custom ceilings and report actual threshold percentages from effective thresholds, not raw ceiling percent. Consider collapsing commit+persist into one API (or enforce call ordering with guardrails). Wire per-session planner cleanup into session deletion lifecycle (or add bounded eviction), and downgrade/remove residual R9-DIAG warn logging. |
| `engine/planner/tests.rs` | 2010 | Planner integration/regression suite validating plan lifecycle, projection math, breadcrumbs, heuristic/alert behavior, and persistent archival layering. | Test module file itself with **53 `#[test]` cases** (no nested inline `#[cfg(test)]`) | Confirmed Claude finding: file is monolithic and should be split by concern. Codex-unique test bug: `test_check_alert_level_respects_ceiling_override()` line 1449 asserts warning text contains `"50%"` after setting ceiling to 50%; this encodes the known threshold-message bug in `planner/mod.rs` (ceiling percent reported as threshold percent). If runtime messaging is corrected to show true soft/medium/hard thresholds, this test will fail and block the fix. Coverage gap: no test currently asserts the exact threshold percentages under ceiling overrides (e.g., soft 25%, medium 40%, hard 50% when ceiling is 50%). | Split to `engine/planner/tests/{mod,plan_management_tests,validation_tests,heuristics_alert_tests}.rs` per existing map. Update alert-message assertions to validate actual threshold math (soft/medium/hard percentages), not ceiling echo text, so tests guard the intended behavior after the planner fix. |
| `proxy/parser/openai.rs` | 515 | OpenAI wire parser for both Chat Completions and Responses APIs, including request/response block extraction, overhead estimation, and thread identity derivation. | None inline (`parser/tests.rs` covers this module) | Confirmed Claude bug: `parse_openai_responses_request()` is missing a `"function_call"` input-item handler, so assistant tool-call history in Responses `input[]` degrades into user-text fallback blocks. Confirmed Claude finding: request parsing does a double deserialize/clone on hot paths (`from_slice` then `from_value(raw.clone())`). Confirmed Claude finding: unknown/unsupported roles are coerced to `Role::User`; this misclassifies OpenAI `developer` role semantics. Confirmed Claude finding: tool call IDs are parsed but not persisted in block metadata, so deterministic ToolUse→ToolResult matching remains blocked pending `tool_use_id` metadata support. Codex-unique concern: `extract_text_content()` fallback stringifies non-text JSON values, which can silently corrupt multimodal content into debug text. | Add a dedicated `"function_call"` arm in Responses request parsing and regression tests for historical function-call inputs. Refactor to a single parse pass where practical, map `developer` explicitly, and thread `tool_use_id` once metadata schema is extended. Tighten `extract_text_content()` fallback to avoid lossy non-text coercion. |
| `proxy/mod.rs` | 213 | Proxy module root: shared `ProxyState`, upstream URL configuration, and `start_proxy()` router/bootstrap path. | **32 lines inline `#[cfg(test)]`** (3 tests) | Confirmed Claude finding: `ProxyState.engine` is public while adjacent internals (`client`, `config`, `dispatcher`) are `pub(crate)` (lines 67-78), creating inconsistent mutation/access boundaries around a core shared state object. Confirmed Claude finding: `chatgpt_codex_url` is hardcoded to a non-versioned backend URL at line 59 with no fallback/probe mechanism. Confirmed Claude note: no `Default` trait despite `new()` being default constructor behavior. | Extract tests to `proxy/tests/proxy_mod_tests.rs`. Normalize field visibility strategy for `ProxyState` and decide whether `chatgpt_codex_url` should be configurable from runtime/env to avoid hardcoded endpoint drift. |
| `proxy/error.rs` | 43 | Canonical proxy error enum used across startup, forwarding, validation, timeout, and parser surfaces. | None (type-only) | Confirmed Claude note: both `ClientBuildFailed` and `UpstreamFailed` wrap `reqwest::Error`; `#[from]` on `UpstreamFailed` means implicit `?` conversions always land there, while `ClientBuildFailed` is construction-site explicit only. No correctness bug found. | No action needed; keep variant distinction documented for call sites that rely on setup-vs-runtime error categorization. |
| `proxy/runaway_guard.rs` | 244 | Rolling-window runaway detector for proxy request volume and context-tool call bursts, including context circuit-breaker timing. | **63 lines inline `#[cfg(test)]`** (3 tests) | Confirmed Claude finding: only context tools have an actual breaker state/query (`context_circuit_remaining()` lines 136-152); proxy hard-limit is advisory-only. Confirmed Claude finding: proxy alert requires BOTH request count and large-body count (line 79), so small-body floods never trigger. Codex-unique asymmetry: proxy path throttles repeated alerts via cooldown unconditionally (lines 85-87), but unlike context path it does not bypass cooldown on hard-limit events; sustained proxy hard-limit bursts can be under-signaled compared to context hard-limit behavior. | Extract tests to `proxy/tests/runaway_guard_tests.rs`. Document intentional asymmetry (if desired) or align behavior by bypassing proxy cooldown on hard-limit conditions and/or adding a proxy breaker/query path. |
| `proxy/hot_patch.rs` | 341 | Persistent patch queue and JSON body patcher applied to outbound Anthropic/OpenAI requests before forwarding. | **147 lines inline `#[cfg(test)]`** (8 tests) | Confirmed Claude bug: top-level `system` patch logic only handles string content (`as_str()` at lines 112-119); Anthropic/OpenAI array/object system formats are skipped silently. Confirmed Claude note: `apply_to_messages()` updates only `block["text"]` in array content (line 176), so non-text block layouts are intentionally out of scope today. Confirmed Claude note: substring replacement semantics (`str::replace`) apply globally per field, not first-match only. | Extract tests to `proxy/tests/hot_patch_tests.rs`. Add array-system patch support mirroring manifest injection behavior, and add explicit test coverage for system-array patching to prevent regression. |
| `proxy/provider_adapter.rs` | 251 | Provider adapter registry/trait layer that dispatches request/response parsing by path and advertises parser capabilities. | **41 lines inline `#[cfg(test)]`** (5 tests) | Confirmed Claude finding: `detect_adapter_for_path()` rebuilds boxed adapter registry every call (lines 201-203), causing avoidable heap allocations on hot routing paths. Confirmed Claude finding: placeholder adapters never match paths (`matches_path()` returns false at lines 148-150), so extension IDs exist but are unreachable via auto-detection. Confirmed Claude finding: placeholder capability flags are aspirational despite parse methods always returning "not implemented". | Extract tests to `proxy/tests/provider_adapter_tests.rs`. Replace per-call adapter allocations with static/`OnceLock` registry or direct path dispatch, and document placeholder adapters as non-routable until real `matches_path()` implementations exist. |
| `proxy/parser/overhead.rs` | 65 | Parser-local utility that estimates non-block token overhead from tools/system/instructions in raw request payloads. | None inline (indirectly covered by parser integration tests) | Confirmed Claude note: bytes/4 heuristic is coarse but currently overwritten by ingest retokenization. Codex-unique edge gap: OpenAI Chat overhead only checks `messages[0]` for `role=system` (lines 46-60); if rewrites produce a valid later system message, overhead undercounts system bytes. | Keep heuristic approach for now, but add direct unit tests for all supported system formats and include a later-system-message case to document/lock current behavior (or broaden scan if accurate overhead becomes important). |
| `proxy/parser/identity.rs` | 180 | Thread identity derivation from explicit IDs with fallback content anchors and transient-wrapper filtering. | None inline (`parser/tests.rs` coverage) | Confirmed Claude finding: normalization helpers are near-duplicate (`normalize_identity_value`, `normalize_anchor_content`, lines 6-31). Confirmed Claude finding: fallback hash uses `DefaultHasher` via `short_hash()` (line 175), which is not a stability-guaranteed contract across Rust changes. Confirmed Claude finding: billing-header filtering logic is duplicated here and in `anthropic.rs`. | Collapse normalization helpers to one parametrized function, move billing-header stripping to a shared parser helper, and switch fallback hashing to the same explicit deterministic hasher selected for parser block IDs. |
| `proxy/parser/mod.rs` | 344 | Parser module root: provider/path detection, parsed request/response contracts, block construction helpers, and unified parse dispatch. | `#[cfg(test)] mod tests;` (external parser suite) | Confirmed Claude finding: `stable_block_id()` and `short_hash()` both depend on `DefaultHasher` (lines 155-169 and 234-237), creating Rust-version fragility for block/identity stability guarantees. Confirmed Claude finding: local `now_iso8601()` wrapper (lines 140-142) is redundant with `crate::util::iso_now()`. Confirmed Claude finding: unknown-path fallback in `parse_request()` does not attempt Responses API parse (lines 299-301). | Move parser hashing to an explicit stable hasher shared with ingest fingerprint fixes, remove redundant time wrapper, and include Responses parser in unknown-path fallback chain (Anthropic -> OpenAI Chat -> OpenAI Responses). |
| `proxy/parser/anthropic.rs` | 410 | Anthropic Messages parser for request/response conversion into canonical blocks, including tool and thinking block handling. | None inline (`parser/tests.rs` coverage) | Confirmed Claude finding: request parse does raw JSON deserialize + typed deserialize clone path (lines 87-93), doubling parse/alloc cost on hot request traffic. Confirmed Claude finding: billing-header filtering is duplicated with identity module (lines 117-124). Confirmed Claude note: `tool_result` carries `tool_use_id` in content but metadata cannot persist that ID yet (line 262 path via `make_tool_block(..., None, ...)`). Codex-unique hot-path cost: each content block in array messages is reparsed with `serde_json::from_value(cb_value.clone())` (line 194), adding per-block clone/deserialize overhead instead of direct field extraction. | Refactor to single-pass parsing where feasible (reuse typed struct data and avoid per-block cloned deserialization), extract shared billing-header helper, and populate `tool_use_id` metadata once `BlockMetadata` schema is extended. |
| `proxy/parser/tests.rs` | 1491 | Monolithic parser integration/regression suite covering provider routing, Anthropic/OpenAI parsing, identity/fingerprint stability, and overhead estimation. | Test module file with **73 test fns** (`74` `#[test]` attrs) | Confirmed Claude finding: H9 is explicitly encoded as expected divergence in `test_h9_thread_identity_diverges_after_early_turn_removal()` (lines 1374-1422, `assert_ne!`), documenting today’s session-flip bug when early anchor turns are archived. Coverage gap: Responses request tests only cover string/message input (`test_parse_openai_responses_request_*`, lines 359-386); there is no request-side `"type":"function_call"` regression despite missing handler in `openai.rs`. Coverage gap: OpenAI role tests do not include `developer` role (chat tests begin line 286; responses message tests line 375), so current `developer -> User` misclassification can regress silently. Codex-unique hygiene issue: duplicate `#[test]` attribute at line 1359 before the H9 doc block leaves stacked test attrs (`1359` and `1373`) on the same function. | Split into `proxy/parser/tests/{routing_tests,anthropic_tests,openai_tests,identity_tests}.rs` for maintainability. After the H9 identity fix, replace divergence assertion with stable-identity expectation (or mark divergence test as `#[ignore]` bug repro). Add request-side Responses `function_call` and OpenAI `developer` role regressions, and remove the redundant `#[test]` attribute. |
| `proxy/rewriter.rs` | 308 | Rewriter orchestration pipeline: session resolution, planner/applicator execution, runtime cleanup, trailing injection, tool injection, and Anthropic sanitization. | `#[cfg(test)] mod tests;` (externalized in `rewriter/tests.rs`) | Confirmed Claude finding: R9-DIAG production `warn!` calls at lines 73-76 and 124-129 fire on pending-plan paths and should not remain warning-level post-regression. Confirmed Claude finding: warm path does a third JSON parse per request (`collect_traffic_signals(path, body)` at line 132 parses once, then rewrite parse at line 188, after parser module already parsed the body). Confirmed Claude finding: `should_inject` is computed at lines 172-173 but the injection branch re-evaluates the full predicate at line 229; redundant and drift-prone if one side changes. | Downgrade R9-DIAG logs to `debug!` (or gate behind feature flag), thread a pre-parsed JSON value into traffic-signal collection to remove extra parse, and use the precomputed `should_inject` boolean at injection site for single-source predicate logic. |
| `proxy/rewriter/trailing.rs` | 130 | Appends warning/breadcrumb trailing text to the last user-facing message across Anthropic, OpenAI Chat, and OpenAI Responses payload shapes. | None (covered by `rewriter/tests.rs`) | Confirmed Claude finding: newline formatting is inconsistent between string and array content paths (`\n\n` at lines 40 and 116 vs `\n` at lines 47 and 122), creating provider-format-dependent output text. Confirmed Claude finding: OpenAI Responses object-input branch clones the entire object at lines 83-86 before mutation, which is avoidable allocation on hot path. | Standardize trailing separator behavior across string/array branches (`\n\n` or documented alternative), and mutate object input in place instead of clone-and-reassign. |
| `proxy/rewriter/signals.rs` | 275 | Extracts current-turn tool traffic signals (`current_turn_files`, `file_mutations`) from request JSON for planner heuristics. | None (covered by `rewriter/tests.rs`) | Confirmed Claude finding: `collect_traffic_signals()` reparses raw bytes at line 21, contributing to per-request triple JSON deserialize in the rewrite path. Confirmed Claude finding: turn indexing is cross-provider asymmetric (Responses/Chat use `idx` at lines 81 and 131; Anthropic uses `idx + 1` at line 176), undocumented and easy to misread during cross-format debugging. Confirmed Claude finding: entry turn assignment uses `call_turn.max(result_turn)` at line 230 (result-turn dominance), which is intentional but undocumented. Codex-unique determinism risk: `build_entries_from_calls()` iterates `HashMap` entries directly (line 224), so `current_turn_calls` ordering is nondeterministic between runs; if multiple same-turn calls target the same file, downstream mutation ordering can drift. | Accept `&Value` (or parsed structure) instead of `&[u8]` to avoid reparse, document turn-index semantics for each provider, and sort built entries deterministically (e.g., by turn then call_id) before downstream mutation detection. |
| `proxy/rewriter/sanitize.rs` | 336 | Anthropic structural sanitizers for orphan tool pairs and role-sequence repair after archival/cleanup mutations. | None (covered by `rewriter/tests.rs`) | Confirmed Claude finding: synthetic user filler text is hardcoded to `"Continue."` at lines 269 and 285, so repeated sanitization can inject user-visible prompt text that is not caller-authored. Confirmed Claude finding: `merge_message_content()` upcasts string content to array form (lines 303-305, 314-319), which is valid but format-changing and may affect cache-key behavior for providers sensitive to shape. | Make synthetic filler text configurable/centralized (or neutral constant with explicit contract), and document/limit content-format normalization so cache-sensitive paths can opt into shape preservation where needed. |
| `proxy/rewriter/payload.rs` | 355 | Applies rewrite decisions to provider JSON payloads (turn removal, content replacement, partial-turn stubs) for Anthropic/OpenAI formats. | None (covered by `rewriter/tests.rs`) | Confirmed Claude bug: `replace_anthropic_content()` forces turn-0/system replacement to `Value::String` (lines 67-71), destroying original array/object system format and invalidating cache stability assumptions. Confirmed Claude bug: `replace_message_content()` replaces only the first text-like block in array content and returns (lines 175-189), leaving stale trailing text blocks when multi-text content is present. Confirmed Claude finding: `apply_partial_turn_stubs_openai_chat()` only applies when `content_index == 0` (lines 268-274), silently ignoring other indices. Confirmed Claude finding: tool-use stub path writes internal `_archived` marker into `input` (lines 340-344), an undocumented wire-shape convention. | Preserve original system content shape when replacing turn 0 (or normalize consistently to provider-preferred array format), update replacement logic to rewrite all text blocks (or enforce/validate single-text invariant), and document or enforce `content_index == 0` constraints for OpenAI Chat stubs. Define/contain `_archived` marker contract to avoid leaking internal semantics unexpectedly. |
| `proxy/rewriter/tests.rs` | 1148 | Rewriter test suite covering payload rewrites, traffic signal extraction, tool-injection gating, partial-turn stubs, and end-to-end applicator→payload scenarios. | Test module file with **38 tests** | Confirmed Claude finding: `test_apply_decisions_anthropic_path()` is weak (lines 290-315) and does not actually assert replacement correctness after remove+replace ordering. Confirmed Claude coverage gap: no regression guard for system array format preservation (`replace_anthropic_content` turn 0) and no regression for multi-text-block replacement behavior (`replace_message_content` first-block-only). Codex-unique cleanup issue: duplicated local `make_block` helper appears twice (lines 702 and 819), adding maintenance noise. | Split by concern (`payload`, `signals`, `pipeline/sanitize`) and add targeted regressions for system-array replacement and multi-text-block replacement. Deduplicate `make_block` helper into shared local test utility. |
| `proxy/capture.rs` | 273 | Request/response capture store for proxy traffic, including SSE buffering, parse-on-capture, status tracking, and block aggregation for ingest/UI. | `#[cfg(test)] mod tests;` (externalized in `capture/tests.rs`) | Confirmed Claude findings: `evict_if_needed()` is hash-iteration based (not FIFO/oldest) and `finalize_streaming()` marks parse failures as `Complete` (status contract mismatch). Additional Codex concern remains: retention cap applies only to `Complete` entries, so long-lived non-complete entries can accumulate under failure-heavy traffic. | Add deterministic retention ordering metadata (timestamp/sequence), mark streaming parse failures explicitly (or differentiate partial completion), and add cleanup policy for stale non-complete exchanges. |
| `proxy/capture/sse.rs` | 275 | SSE final-response reconstruction helpers for Anthropic, OpenAI Chat, and OpenAI Responses streaming flows. | None inline (covered by `capture/tests.rs`) | Confirmed Claude bug: tool/function streaming output is dropped across all providers (text-only reconstruction paths ignore tool deltas/calls). Confirmed Claude concern: reconstruction collapses multi-block structure into single text output and can lose non-text semantics. Codex additionally flags permissive `None` gating (`empty text && zero usage`) as a drop condition for valid non-text terminal payloads. | Reconstruct tool/function events in SSE paths (not just text deltas), preserve multi-block semantics where possible, and add regressions for tool-only streaming traces in all three provider formats. |
| `proxy/capture/tests.rs` | 227 | Capture store and SSE reconstruction tests covering basic request/response capture, endpoint detection, H9 identity override, and text streaming happy paths. | Test module file with **12 tests** | Confirmed Claude gap: no test covers SSE tool/function streaming reconstruction, leaving the `capture/sse.rs` bug unguarded. Also missing assertions for retention ordering and streaming parse-failure status behavior. | Add SSE tool-use/function-call tests (Anthropic + OpenAI variants), plus store-level tests for eviction ordering and finalize-streaming failure status semantics. |
| `proxy/handler.rs` | 591 | Main proxy request/response coordinator: routing, zstd handling, hot patches, context rewrite, capture wiring, interception, runaway guard, and upstream forwarding/streaming. | `#[cfg(test)] mod tests;` (externalized in `handler/tests.rs`) | Confirmed Claude findings: duplicate request IDs (span field vs runtime UUID), duplicate parse cost (rewrite parse + capture parse), and residual DIAG warning on normal streaming completion. Confirmed Claude robustness concern: `uri_path` is reconstructed from `upstream_url` by brittle string slicing in interception path, despite already-known request path. | Use one request ID source and record it into span fields, reuse parsed request for capture when possible, downgrade DIAG warning, and pass canonical request path through call chain instead of re-parsing URL strings. |
| `proxy/handler/exchange.rs` | 88 | Finalization bridge that emits response events and feeds captured exchanges into `engine.ingest` on `spawn_blocking`. | None inline (behavior not covered in `handler/tests.rs`) | Confirmed Claude findings: DIAG `warn!` logs are still on hot path and JoinError fail-open currently reports capture-applied semantics even when ingest task failed. | Downgrade DIAG logs to `debug!` and separate “response complete” from “ingest applied” signaling on task failures so UI/event stream does not misreport state application. |
| `proxy/handler/headers.rs` | 105 | Header sanitation/conversion helpers for proxy request and response forwarding (hop-by-hop stripping, zstd detection, debug logging). | None inline (tested indirectly in `handler/tests.rs`) | Confirmed Claude finding: response-side Connection-token nominated header stripping is missing (RFC 7230 gap). Codex also keeps a log-hygiene concern: redaction list is minimal and may leak other auth-like headers at debug level. | Add response Connection-token stripping support and extend sensitive-header redaction policy beyond `authorization`/`x-api-key`. |
| `proxy/handler/routing.rs` | 121 | Upstream-selection and API-path normalization utilities (Anthropic/OpenAI/ChatGPT Codex routing). | None inline (tested in `handler/tests.rs`) | Confirmed Claude note: `anthropic-beta` alone is not considered in Anthropic routing signals, and unsupported bearer+path combinations fall through to OpenAI defaults. Codex interprets this as acceptable but under-documented precedence behavior rather than a hard bug. | Document precedence explicitly (header + path interaction) and add tests for `anthropic-version`/`anthropic-beta` edge routing expectations. |
| `proxy/handler/tests.rs` | 401 | Unit tests for handler helper modules: routing decisions, path normalization, header stripping/conversion, zstd detection, and hot-patch-on-decompressed-body behavior. | Test module with **28 `#[test]` cases** | Confirmed Claude gaps: missing `anthropic-version`-only routing coverage and no regression for response Connection-token stripping behavior. Integration-level handler flow assertions remain limited. | Add targeted tests for `anthropic-version` routing and response Connection-nominated header stripping; consider a small integration harness for orchestrator-level flow checks. |
| `proxy/interceptor.rs` | 373 | Non-streaming context-tool interception controller: extract calls, dispatch tools, optional upstream re-invoke recursion, mixed-response stripping, and circuit-breaker fallback. | `#[cfg(test)] mod tests;` (externalized in `interceptor/tests.rs`) | Confirmed Claude bug: recursive `reinvoke_with_results` dispatch bypasses `runaway_guard.record_context_tool_call()`, so depth>0 context-tool calls evade runaway accounting. Confirmed Claude findings: recursive path reparses request JSON each depth and carries high-arg-count function signatures (`#[allow(clippy::too_many_arguments)]`) that should be collapsed into a shared context struct. Codex-unique concern remains: reinvoke header forwarding allowlist is narrow and may drop provider-relevant headers during recursive upstream calls. | Use one guarded-dispatch helper for all depths, thread parsed request state instead of reparsing raw bytes, and introduce a `ReinvokeCtx` struct to reduce argument sprawl. Document/expand reinvoke header forwarding policy and add regression tests for recursive guard accounting. |
| `proxy/interceptor/response.rs` | 177 | Helper transforms for interception flow: context-only detection, context-call stripping, circuit-breaker body shaping, and assistant-response append for reinvoke. | None inline (covered by `interceptor/tests.rs`) | Confirmed Claude bug: `is_context_only_response()` chat `has_text` path only checks string `content`, so array-typed text content can be misclassified as context-only and trigger unintended reinvoke. Confirmed Claude note: CodexProxy-only fallback/append behavior should be documented inline to prevent misuse. Codex-unique concern remains: `append_assistant_response()` only appends for array-shaped Responses `input`; object/string forms are ignored, which can drop assistant context for reinvokes. | Extend text detection to array content blocks, add explicit CodexProxy-only behavior comments, and normalize Responses `input` to an appendable array form before reinvoke mutation. Add regressions for array-content chat messages and non-array Responses input shapes. |
| `proxy/interceptor/tests.rs` | 373 | Interceptor unit tests for response-shape helpers and reinvoke guardrails (depth/timeout fail-open). | Test module with **16 tests** (including 2 async reinvoke guard tests) | Confirmed Claude coverage gap: tests are helper-heavy and do not exercise `try_context_tool_interception()` entry behavior (runtime bypass, circuit pre-check, context-only vs mixed branches, reinvoke fallback-to-strip). Confirmed Claude gap: no regression for recursive runaway-guard accounting and no array-content regression for `is_context_only_response()`. Codex-unique gap remains: no coverage for non-array Responses `input` behavior in `append_assistant_response()`. | Add entrypoint-focused tests for `try_context_tool_interception`, plus regressions for recursive runaway accounting, array-content `has_text`, and non-array Responses `input` append behavior. |
| `proxy/context_api.rs` | 365 | Internal `/_aperture/*` HTTP router for context tools, health/events, and IPC pass-through, including semaphore-serialized tool dispatch. | `#[cfg(test)] mod tests;` (externalized in `context_api/tests.rs`) | Confirmed Claude findings: residual R9-DIAG `warn!` on plan operations, one-line alias wrapper (`context_tools_disabled_response`) with little value, stale Tauri-specific SSE error message, and magic `"__legacy__"` fallback string usage. Codex-unique concern remains: route dispatch currently allows non-`OPTIONS` methods through context/ipc handlers without strict method gating. | Downgrade/remove DIAG warn logging, inline/remove the alias wrapper, update SSE error text, and extract a named legacy-session constant. Add explicit method guards (`GET` for health/events, `POST` for context/ipc) and corresponding tests if that contract is intended. |
| `proxy/context_api/tests.rs` | 375 | Context API tests covering route matching, tool dispatch error/success paths, session override, circuit-breaker behavior, and semaphore-based concurrency regression checks. | Test module with **14 async tests** | Confirmed Claude findings: inconsistent `Arc::new((*state).clone())` pattern in one test, timing-fragile 50ms sleep assertion in semaphore serialization test, and brittle string-fragment assertion for preview output text. Confirmed Claude/Codex gap: missing contract tests for OPTIONS/CORS and `/events` endpoint behavior (and method semantics if enforced). | Normalize shared-state cloning style, replace timing sleeps with deterministic synchronization primitives, and relax brittle string assertions to structural output checks. Add explicit tests for OPTIONS/CORS and `/events` route behavior. |
| `proxy/ipc_api.rs` | 341 | HTTP IPC command dispatcher mapping `/_aperture/ipc/{command}` requests to engine and hot-patch operations with JSON argument helpers. | Inline helper tests under `#[cfg(test)]` (5 tests) | Confirmed Claude bugs: `engine_pin_block` silently unpins on unknown position strings, `parse_zone` can silently convert typos into `Zone::Custom`, and core 25-command dispatcher behavior is largely untested (helper parsers only). Confirmed Claude smell: repeated engine-none boilerplate in specific handlers. Codex-unique concerns remain: no explicit method validation in dispatcher path and `string_array_arg()` silently drops non-string elements via `filter_map`, allowing implicit partial-arg corruption. | Add strict pin-position parsing (error on unknown values), trim/normalize zone parsing before built-in/custom branching, and add command-dispatch tests (not only helper parser tests). Factor shared engine access helpers to reduce boilerplate; enforce request method contract and strict array-arg typing if API surface requires it. |
| `metacog/mod.rs` | 86 | Metacog facade: module exports/re-exports and `select_runtime(kind, path)` adapter factory. | **37 lines inline `#[cfg(test)]`** (5 tests) | Confirmed Claude finding: `select_runtime()` line 44 detects Anthropic passive mode via `path.contains("/messages")` only. If passive mode is used with other Anthropic endpoints (for example `/v1/count_tokens`), it incorrectly selects OpenAI-style manifest injection path. | Extract tests to `metacog/tests/mod_tests.rs` (or shared `metacog/tests.rs`). Replace substring detection with shared provider/path routing helper so passive runtime format detection stays aligned with proxy routing logic. |
| `metacog/passive.rs` | 147 | `PassiveRuntime` implementation: no tool surface, only manifest injection for Anthropic/OpenAI request shapes. | **76 lines inline `#[cfg(test)]`** (7 tests) | No correctness bug found. Confirmed Claude assessment: behavior is intentionally no-op except `inject_manifest()`, and OpenAI path correctly prefers `input` over `messages` when both APIs are supported. | Extract tests to `metacog/tests/passive_tests.rs`. Keep runtime no-op contract documented in trait-level comments. |
| `metacog/claude_mcp.rs` | 274 | Anthropic/MCP runtime adapter: extracts context tool calls, injects tool results, strips context tool history, injects manifest. | **143 lines inline `#[cfg(test)]`** (8 tests) | Confirmed Claude finding: `inject_results()` lines 114-117 always appends a new `"role":"user"` message and assumes later sanitization merges/fixes adjacency; this creates an implicit ordering dependency on cleanup pipeline. Codex-unique robustness bug: `extract_context_calls()` lines 68-72 accepts missing `tool_use` IDs via `unwrap_or("")`; malformed tool blocks become dispatchable calls with empty IDs, and `inject_results()` then emits `tool_result.tool_use_id=""`, which can produce invalid Anthropic payloads or orphaned results. | Extract tests to `metacog/tests/claude_mcp_tests.rs`. Guard `extract_context_calls()` to skip/log `tool_use` blocks with missing/empty IDs, and add regression test ensuring malformed `tool_use` blocks are ignored instead of dispatched. Document sanitizer ordering dependency where `inject_results()` is invoked. |
| `metacog/preview.rs` | 368 | Preview extractor for blocks: regex identifier mining + content head truncation + summary-line formatting. | **165 lines inline `#[cfg(test)]`** (9 tests) | Confirmed Claude bug: `extract_head()` line 195 truncates by byte index (`String::truncate(MAX_PREVIEW_CHARS)`), which panics when cutoff lands inside a multibyte UTF-8 scalar. Confirmed Claude dead code: `RE_IMPORT` at line 34 is compiled but never read by `extract_identifiers()`. Codex agrees the role-gated file-path extraction (lines 159-167) is a design choice, not a correctness defect. | Fix truncation with char-boundary-safe indexing (`char_indices().nth(...)`). Remove unused `RE_IMPORT` or implement import extraction and add tests. Extract tests to `metacog/tests/preview_tests.rs`. |
| `metacog/runtime.rs` | 442 | Shared runtime contracts: tool definition/call/result types, tool-name classifiers, runtime detection, and standard context-tool schema definitions. | **118 lines inline `#[cfg(test)]`** (8 tests) | Confirmed Claude findings: `is_intercepted_context_tool_name()` (line 77) has no direct test coverage, `detect_runtime()` includes over-broad `path_lower.contains("/responses")` (line 184) that can misclassify unrelated endpoints, and `context_tools_mode()` re-reads env var on every call (line 157). Codex-unique finding: config parse is fail-open by default (`parse_context_tools_mode` returns `Enabled` for unknown strings), so typos in `APERTURE_CONTEXT_TOOLS_MODE` silently re-enable tools instead of surfacing misconfiguration. | Extract tests to `metacog/tests/runtime_tests.rs` and add explicit tests for `is_intercepted_context_tool_name`. Narrow `/responses` matching to supported API paths. Decide whether env mode should be cached (`OnceLock`) and whether invalid mode strings should warn/fail closed. |
| `metacog/tools/plan.rs` | 288 | Plan tool control flow: normalize/validate args, parse stage/append/preview/commit/discard op, and format staged-plan previews. | Tested via `metacog/tools/tests.rs` (no inline `#[cfg(test)]`) | Confirmed Claude findings: `parse_plan_control_op()` consumes pre-normalized `arguments` (line 216), `format_plan_preview()` does signed-to-unsigned cast (`(-plan.token_delta) as u32`, lines 71-72), and malformed string payloads in `normalize_plan_arguments()` fall through to later serde errors without early diagnostic. Codex-unique edge case: `parse_plan_control_op()` is case-sensitive and whitespace-trim only, so model outputs like `"Commit"`/`"APPEND"` fail validation even though semantics are clear. | Switch token-delta formatting to `unsigned_abs()`. Either normalize `control.op` (`to_ascii_lowercase`) or explicitly document strict lowercase contract in tool description. Keep normalization before control parsing or add a comment explaining current order dependency. |
| `metacog/tools/tests.rs` | 381 | Tool-level test suite for preview/read/search/status and plan dispatch/normalization/error behavior. | Test module file with **17 tests** | Confirmed Claude findings: no coverage for `PlanControlOp::append|preview|commit|discard`, no regression for `scope: \"all\"` behavior in `context_search`, and `mock_budget()` duplicates threshold constants (lines 40-57). Codex-unique coverage gap: no test asserts `control.op` case sensitivity, so a future lowercasing/strictness change would be unguarded. | Add plan-control lifecycle tests (append/preview/commit/discard), add `scope: "all"` behavior regression, and replace `mock_budget` hardcoded thresholds with `check_budget()`. Add explicit test for desired `control.op` casing behavior (strict vs normalized). |
| `metacog/codex_proxy.rs` | 521 | OpenAI-side runtime adapter for context tools: detects chat vs responses format, injects tools/results/manifests, extracts context calls, and strips ephemeral tool history. | **299 lines inline `#[cfg(test)]`** (14 tests) | Confirmed Claude findings: `inject_tools()` bypasses `self.tool_definitions()` (line 58), `detect_format()` uses broad `path.contains("/responses")` matching (line 40), `inject_results()` drops `ContextToolResult.is_error` (lines 163-167/177-181), `extract_openai_tool_call()` requires stringified args and silently drops pre-parsed object args (line 229), and repeated `inject_tools()` calls duplicate entries with no dedup guard (line 74 extend). Codex-unique correctness risk: Responses extraction accepts missing `call_id` (`unwrap_or("")` at lines 116-119), so malformed function calls become executable with empty IDs, then result injection emits ambiguous `call_id: ""` outputs that cannot be safely paired. | Route `inject_tools()` through `self.tool_definitions()`, tighten format detection to canonical endpoints, propagate tool error state in injected outputs, and support object-shaped arguments during call extraction. For Responses calls, reject/skip empty `call_id` with explicit logging and add regression tests for malformed IDs/object arguments/dedup behavior. |
| `metacog/tools.rs` | 752 | Shared implementation for the five context tools plus dispatch, validation, search scoring/snippets, and output shaping limits. | Tests in `metacog/tools/tests.rs` via `#[cfg(test)] mod tests;` | Confirmed Claude bugs: `context_search_with_limits()` never changes data source by `scope` (lines 356-388), so `"all"` is a no-op despite schema/docs; `extract_search_snippet()` slices `&snippet[..max_len]` by bytes (line 668), which can panic on UTF-8 boundaries. Confirmed Claude smells: third `format_tokens()` copy (line 741) and `describe_mutation()` uses `{:?}` for zone names (line 724). Confirmed Claude contract gap: `context_status_with_limits()` ignores `_session_id` and `_limits` (lines 423-431), so output is neither session-aware nor limit-aware. Codex-unique note: plan normalization runs twice in the plan path (`validate_tool_call` line 487 and `context_plan` in `plan.rs` line 215), adding avoidable parse/clone work and potential drift if logic diverges. | Implement real `scope: "all"` semantics (or remove it from schema/output), make snippet truncation char-boundary-safe, and extract shared `format_tokens` utility. Document/clean up `context_status_with_limits` unused parameters or wire them to behavior. Thread normalized plan args through dispatch to avoid double normalization. |
| `events/types.rs` | 64 | Event contract surface (`ApertureEvent`) and event-channel constants shared by Tauri/broadcast dispatchers. | None (correct for pure type definitions) | Confirmed Claude architectural coupling: channel selection lives in `events/dispatcher.rs::emit` match, not in event type metadata, so new high-frequency variants require manual dispatcher updates. No correctness bug in this file. | No immediate code change needed; document channel-routing ownership and consider central routing helper/trait if event variants grow. |
| `events/broadcaster.rs` | 116 | Broadcast-backed dispatcher for standalone proxy/SSE mode (`tokio::broadcast` + `DynDispatcher` bridge). | **55 lines inline `#[cfg(test)]`** (3 tests) | Partial disagreement with Claude: `sender.send()` at line 33 does **not** fail on buffer overflow; in `tokio::broadcast`, overflow is surfaced as `RecvError::Lagged` on receivers, not a sender error. Confirmed capacity pressure concern: fixed 256 buffer can still cause lag/drop for slow subscribers under high `ResponseStreaming` volume, but sender-side logging cannot detect those drops directly. | Extract tests to `events/tests/broadcaster_tests.rs`. Add docs/metrics around receiver lag behavior (consumer-side `RecvError::Lagged`) and validate whether capacity 256 is sufficient for expected streaming burst profiles. |
| `events/dispatcher.rs` | 189 | Tauri `EventDispatcher<R>` plus type-erased `DynDispatcher` abstraction for proxy handlers and alternate transports. | None (broadcaster tests only cover `DynDispatcher::from_fn`) | Confirmed Claude duplication smell: six convenience emitters are duplicated in both `EventDispatcher` and `DynDispatcher` (lines 39-93 and 128-182). Codex-unique operational concern: success-path debug log includes full `?event` payload at line 35, which can emit full block content/token metadata and generate high log volume on hot event streams. | Introduce a shared emitter extension/helper to collapse duplicated convenience methods, and reduce success-path log payload (`event_type`/ids only) or guard full event logging behind a verbose flag. |
| `events/mod.rs` | 9 | Events module root exporting broadcaster/dispatcher/type submodules. | None | No correctness issues; matches minimal module-root pattern. | None. |
| `mcp/mod.rs` | 4 | MCP module root (`server` + test module wiring). | Tests delegated to `mcp/tests.rs` | No issues found; structure is clear and idiomatic. | None. |
| `mcp/server.rs` | 352 | JSON-RPC stdio MCP server: method dispatch, proxy forwarding for tool calls, and plan-session affinity helpers. | No inline tests (covered by `mcp/tests.rs`) | Confirmed Claude perf issue: `reqwest::blocking::Client::new()` is recreated on every `call_proxy()` invocation (line 68), losing connection reuse. Confirmed Claude cleanup issue: `should_pin` in `with_plan_session_hint()` (line 160) is redundant because every supported op is listed in the match. Codex-unique reliability bug: `send_response()`/`send_error()` use `expect()` on serialize/write/flush (lines 41-44 and 57-60), so a broken stdout pipe can panic and hard-stop the MCP server instead of graceful shutdown. | Reuse a long-lived blocking `Client` (constructed once in `run_stdio_server` and passed through call path), remove redundant `should_pin`, and convert stdout write path to fallible logging/early-return instead of panics. |
| `mcp/tests.rs` | 138 | Unit tests for MCP helper functions (`tool_api_path`, `tool_definitions`, plan-op/session-affinity helpers). | Test module file with **9 tests** | Confirmed Claude coverage gap: no explicit regression for unknown `control.op` fallback (`foo -> preview`) and no edge cases around payload-inference branches in `infer_plan_control_op()`. Codex-unique gap: no tests for `run_stdio_server` dispatch/error behavior (understandable for stdio loop, but leaves JSON-RPC envelope guarantees untested). | Add helper-level edge tests for unknown op and payload shape variants; consider extraction of message-dispatch logic into testable pure function to validate stdio method routing without integration harness. |
| `terminal/error.rs` | 28 | Terminal command error enum plus string-only serde serialization for Tauri IPC responses. | None (type-only) | No correctness issues found. | None. |
| `terminal/session.rs` | 217 | PTY session wrapper with child process, writer/master handles, reader thread, resize/write/kill lifecycle. | **106 lines inline `#[cfg(test)]`** (2 tests) | Confirmed Claude API inconsistency: `write()`/`resize()` return `Result<(), String>` (lines 78 and 85) while surrounding surface uses `TerminalError`, forcing remap at call sites. Confirmed Claude smell: `id` field is public but unused with `#[allow(dead_code)]` (lines 23-24). Codex-unique lifecycle risk: `kill()` does unconditional `handle.join()` after `child.kill()` (lines 97-101); if PTY reader remains blocked on some platforms, command teardown can hang. | Extract tests to `terminal/tests/session_tests.rs`. Return `TerminalError` directly from session methods, remove/privatize dead `id` field, and make reader-thread shutdown bounded (timeout or non-blocking join strategy) to avoid hanging kill/drop paths. |
| `terminal/mod.rs` | 405 | Terminal command module: PTY spawn/configuration, provider env plans, session map management, and Codex bridge lifecycle commands. | **74 lines inline `#[cfg(test)]`** (6 tests) | Confirmed Claude bug: `kill_session()` maps mutex lock failure to `SessionNotFound` (line 270), conflating lock poisoning with missing session ID. Confirmed Claude concern: unknown provider in `provider_env_plan()` silently yields empty env plan (line 71), so proxy bypass can happen without operator signal. Confirmed Claude concern: invalid `APERTURE_PORT` parse silently falls back to default (lines 168-171 and 197-200). Codex-unique contention risk: `send_input()` holds global `sessions` mutex while doing blocking PTY write/flush (lines 220-229), so one slow write can stall all session operations. | Extract tests to `terminal/tests/mod_tests.rs`. Fix error variant on lock failure, add warning/validation for unknown providers and invalid `APERTURE_PORT`, and reduce lock scope in `send_input` (e.g., per-session interior lock or move I/O out of global map lock). |
| `terminal/codex_bridge.rs` | 588 | Codex subscription bridge thread: tails `~/.codex/history.jsonl`, polls `codex app-server thread/read`, converts turns to blocks, emits events, and ingests into engine. | **94 lines inline `#[cfg(test)]`** (4 tests) | Confirmed Claude bug: no timeout around `wait_with_output()` in `fetch_thread_blocks()` (lines 309-311), so a hung `codex app-server` blocks the bridge loop and `stop()` join. Confirmed Claude correctness risk: `make_block()` uses `Uuid::new_v4()` IDs (line 458), so stable content gets new IDs every poll, weakening continuity semantics. Confirmed Claude gap: extraction ignores `toolCall`/`toolResult` item types (lines 369-405), dropping tool-chain context. Codex-unique startup blind spot: `cursor` initializes to current file length (line 61), so bridge startup ignores existing history entries and won’t poll any active prior session until a new line is appended. Codex-unique event-correlation risk: request IDs are second-granularity timestamps (line 165), so rapid emits in the same second can collide per session. | Extract tests to `terminal/tests/codex_bridge_tests.rs`. Add subprocess timeout/kill strategy, derive stable deterministic block IDs, and parse tool call/result items (or explicitly document unsupported types). On startup, backscan history tail (or read last entry once) to seed active session. Include higher-entropy request IDs (monotonic counter or UUID suffix). |
| `lib.rs` | 184 | Tauri frontend entrypoint and proxy bootstrap orchestration (health check, detached proxy spawn, terminal command registration). | None (entrypoint file) | Confirmed Claude startup inefficiency: `is_proxy_healthy()` constructs a new blocking `reqwest` client each call (line 95), and `wait_for_proxy()` can call it repeatedly. Codex-unique robustness bug: Unix `pre_exec` closure calls `libc::setsid()` but ignores return value (lines 125-127), so `setsid` failure is silently treated as success and child may remain coupled to parent session. Minor panic edge: `current_exe.parent().unwrap()` (line 154) can panic if parent path is unavailable. | Reuse a shared health-check client during startup polling. In `pre_exec`, check `setsid()` return and propagate `std::io::Error::last_os_error()` on failure; replace `unwrap()` with safe fallback when resolving sibling binary path. |
| `util.rs` | 192 | Shared runtime utilities for env loading, tracing init, proxy port parsing, panic diagnostics, ANSI stripping, and ISO timestamp generation. | **58 lines inline `#[cfg(test)]`** (8 tests) | Confirmed Claude note: this module is the canonical `iso_now()` source and wrapper duplicates in other modules should be removed. Confirmed Claude behavior: `get_proxy_port()` silently falls back to default on parse failure (lines 40-43). Codex-unique parser gap: ANSI regex strips OSC sequences terminated by BEL (`\x07`) but not ST (`ESC \\`) termination (line 92), so some terminals’ OSC escapes may leak through unstripped. | Extract tests to `tests/util_tests.rs` in A.1. Remove duplicate `iso_now` wrappers across modules and decide whether to log invalid `APERTURE_PORT`. Expand ANSI regex/tests to cover OSC-ST termination if terminal output cleanup requires full OSC support. |
| `main.rs` | 6 | Tauri binary shim calling `aperture_lib::run()`, with Windows subsystem attribute for release builds. | None | No issues found. | None. |
| `bin/aperture_mcp.rs` | 8 | MCP standalone binary shim delegating to `mcp::server::run_stdio_server()`. | None | No issues found; thin entrypoint is appropriate. | None. |
| `bin/aperture_proxy.rs` | 34 | Standalone proxy binary bootstrapping crash diagnostics/logging/env, event broadcaster, engine, and async proxy server start. | None | Confirmed Claude assessment: clean startup flow and correct non-Tauri wiring (`None` app handle path). No new issues found. | None. |

---
